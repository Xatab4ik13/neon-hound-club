-- Промокод на группу товаров: например «любые носки» (белые + розовые).
-- product_ids — список товаров, на любой из которых действует код.
-- Скидка применяется к 1 шт. самого дорогого подходящего товара в корзине.
-- Старая колонка product_id остаётся для одиночных товарных промокодов.
ALTER TABLE promo_codes
  ADD COLUMN IF NOT EXISTS product_ids uuid[];
