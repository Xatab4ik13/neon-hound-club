/**
 * Единая точка иконок проекта.
 *
 * Этап 0: всё пока ре-экспортится из lucide-react — визуально ничего не меняется.
 * Дальше будем по одной заменять иконки на Streamline Core Solid прямо здесь,
 * не трогая сами компоненты, которые их используют.
 */
export * from "lucide-react";
export type { LucideIcon, LucideProps } from "lucide-react";
